#!/usr/bin/env bash -e
# ~/.gorc
[ -z $DEBUG ] || set -x

GOPATH=$HOME/.go

[ -d $GOPATH ] || mkdir -p $GOPATH
[ `which brew 2>/dev/null` ] && GOROOT=$(brew --prefix go)

PATH=$GOPATH/bin:$GOROOT/bin:$PATH

export GOPATH GOROOT PATH
