#!/usr/bin/env bash -e
# ~/.gorc
[ -z $DEBUG ] || set -x

# Search several directories here just in case I place it in some odd spot.
[ -d /usr/local/go ] && export GOROOT=/usr/local/go
[ -d /opt/local/go ] && export GOROOT=/opt/local/go

[ -z $GOROOT ] || export PATH=$GOROOT/bin:$PATH
